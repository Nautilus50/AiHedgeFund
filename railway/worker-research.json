{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm exec turbo run build --filter=@arf-os/worker-research...",
    "watchPatterns": ["apps/worker-research/**", "packages/**", "pnpm-lock.yaml", "package.json", "turbo.json"]
  },
  "deploy": {
    "startCommand": "pnpm --filter @arf-os/worker-research start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
