{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm exec turbo run build --filter=@arf-os/worker-backtest...",
    "watchPatterns": ["apps/worker-backtest/**", "packages/**", "pnpm-lock.yaml", "package.json", "turbo.json", "railway/worker-backtest.json"]
  },
  "deploy": {
    "startCommand": "pnpm --filter @arf-os/worker-backtest start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
